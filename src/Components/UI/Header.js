import "./Header.css";

const Header = () => {
  return (
    <div className="header_class">
      <p className="header_desc">
        Track your day to day expenses by adding in the app.
      </p>
      <h2 className="header_title">Expense Tracker</h2>
      <p className="header_desc">
        Click on 'Add New Expense' button to start adding expenses.
      </p>
    </div>
  );
};

export default Header;
